namespace Editor.EditorCore
{
    /*
     * to have undo
     *
     * - instead of calling extrude mesh in overlay, set it to dirty
     * here, check if mesh is set to dirty, if dirty extrude
     */
    public class BezierSplineChanged
    {

    }
}
